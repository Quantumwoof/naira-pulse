# DEMO_MODE=1 skips Bedrock/AWS and runs deterministic tool logic (default).
DEMO_MODE=1

# Optional: AWS region when deploying to AgentCore / Bedrock
# AWS_REGION=us-east-1
# AWS_DEFAULT_REGION=us-east-1
